<p>Hello {{ user.name }}!</p>
<p>You have requested to reset your password on OpenMapGenerator.</p>
<p>To set a new password, go to <a href="{{ absolute_url(path('security.resetpassword', { 'key': recoverKey })) }}">{{ absolute_url(path('security.resetpassword', { 'key': recoverKey })) }}</a></p>
<p>If you did not initiate this request, please ignore this message.</p>
<p>Have a nice day with OpenMapGenerator!</p>
<hr />
<p><img alt="OMG Logo" src="cid:logo" style="vertical-align: middle;"> OpenMapGenerator Password Recovering Service</p>
